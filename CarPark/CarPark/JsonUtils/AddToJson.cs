using System;
using System.Collections.Generic;
using System.IO;
//using System.Text.Json;

using ParkingLotClass = CarPark.ParkingLot.ParkingLot;

namespace CarPark.JsonUtils//CarPark.JsonUtils.AddToJson
{
    public class AddToJson
    {
        /*internal static void AddToJsonFile()//TODO: to understand how to give List params
        {
            var filePath = Path.Combine(AppContext.BaseDirectory, "path.json");
            
        
            string json = JsonSerializer.Serialize(ParkingLotClass.ParkingSpot);
            Console.WriteLine("json:"+json);
            //var filePath = Path.Combine(AppContext.BaseDirectory, "path.json");
            Console.WriteLine("filePath:" + filePath);
            File.WriteAllText(filePath, json);
        }*/
    }
}